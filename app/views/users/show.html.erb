<p id="notice"><%= notice %></p>
<div class="child row">
  <div class="wrapper col-md-6 col-md-offset-3 col-sm-10 col-sm-offset-1">
    <div class="blog-text">
      <p class="left"><strong>名前</strong></p>
      <p class="left"><%= @user.name %></p>
      <p class="left"><strong>メール</strong></p>
      <p class="left"><%= @user.email %></p>
      <p class="left"><strong>パスワード</strong></p>
      <p class="left"><%= @user.password %></p>
      <p class="right">
        <strong>作成時間:</strong>
        <%= @user.created_at.strftime("%y/%m/%d %p %l:%M") %>
      </p>
    </div>
    
    <p>フォロー状況</p>
    <dv>
       <p>フォロワー・ユーザー</p>    
       <%= render partial: 'users/follower', locals: { user: @user } %>
    </dv>
    <br>
    <dv>
       <p>フォロード・ユーザー</p>   
       <%= render partial: 'users/followed', locals: { user: @user } %>
    </dv>
    <%= link_to '戻る', users_path %>
  </div>
</div>